{
  "name": "onedrive-netflix-app",
  "version": "1.0.0",
  "description": "OneDrive Netflix-style file explorer and media player",
  "scripts": {
    "build": "cd frontend && yarn install && yarn build",
    "dev": "cd frontend && yarn start",
    "vercel-build": "cd frontend && yarn install && yarn build"
  },
  "engines": {
    "node": "20.x"
  }
}